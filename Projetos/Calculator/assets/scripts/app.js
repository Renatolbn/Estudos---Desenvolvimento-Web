const defautResult = 0;
let currentResult = defautResult;

function add(n1, n2) {
    const result = n1 + n2;
    return result;
}

addBtn.addEventListener();

outputResult(currentResult, calculationDescription);

// Javascript lê o código de cima para baixo.